/* eslint-disable class-methods-use-this */
/* eslint-disable padding-line-between-statements */
import AbstractCircle from '../adapters/AbstractCircle';
import '../../css/circle.css';

export default class Circles extends AbstractCircle {
	drawCircles(obj) {
		super.drawCircles(obj);
	}
}
